# Copy this file to `.env` in the repo root (docker-compose will read it).
#
# WARNING: Do not commit real secrets.

JWT_SECRET=dev_change_me
INTERNAL_SERVICE_TOKEN=internal_dev_change_me
WEBHOOK_SECRET=webhook_change_me

# Optional: creates an admin user on `patient-service` startup
ALLOW_ADMIN_REGISTER=false
ADMIN_EMAIL=admin@local.test
ADMIN_PASSWORD=admin12345

